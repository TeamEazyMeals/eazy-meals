import React from "react"

const RecipeCategories=({tags})=>{
      
        
       
    
    return(
        <>
            {/* {Object.values(tags)
            .filter((tag)=>(tag==="chicken"))
        .map((recipeName)=>{
           return( 
               <h1>chicken Recipes</h1>
               <ul>
                   <li>{recipeName}</li>
               </ul>
           )
        })} */}
        
        </>
    )
}
export default RecipeCategories;
